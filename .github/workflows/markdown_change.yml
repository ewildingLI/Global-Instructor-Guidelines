name: Markdown Change Workflow

on:
  push:
    paths:
      - '/*.md'  # Triggers on any .md file in the root
     
jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test Step
        run: echo "Markdown file has changed!"